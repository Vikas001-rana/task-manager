# TODO

- [x] Add Railway-friendly production `start` command (bind `vite preview` for Railway) in `package.json`.
- [x] Add `preview` host binding so app listens on Railway-assigned interface.
- [ ] Commit-ready change: verify `dist/` is used (no Vite dev server on prod).
- [x] Re-run `npm run build` locally and ensure it produces `dist/`.
- [x] Share updated `package.json` start command with the user for final deployment.


